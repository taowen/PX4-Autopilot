 这份代码定义了`RtlMissionFast`类，它继承自`RtlBase`和`MissionBase`类，用于处理快速返航任务的业务逻辑。主要业务操作包括：

1. **任务激活与状态检查**：在`on_activation`方法中，代码更新了家位置和全局位置信息，检查当前任务项的有效性，并根据飞行器状态（如是否着陆）调整任务状态。

2. **任务项设置与执行**：`setActiveMissionItems`方法根据飞行器类型和状态（如是否为旋翼机、是否为VTOL等）设置任务项，包括位置、航向等，并将这些设置点发送到导航系统。同时，它处理了着陆命令，更新了任务项类型，并重置了任务项到达状态。

3. **返航时间估计**：`calc_rtl_time_estimate`方法计算并返回返航时间估计，尽管在这个实现中，它简单地返回了一个无效的时间估计，表明这个功能可能还未完全实现。
